rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Default: deny everything. We will open specific collections intentionally.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
