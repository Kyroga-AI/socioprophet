# Archived — not deployed

As of 2026-08-20, this `marketing/` surface is decommissioned and no longer serves any public-facing
function. It is not wired to any Firebase Hosting target (removed from `firebase.json` and
`.firebaserc`), so `firebase deploy` from this repo can no longer publish it.

`socioprophet-website` (GitHub: `Kyroga-AI/socioprophet-website`) is the site being taken forward and
is the sole owner of the `socioprophet-marketing` Firebase Hosting site (the two previously collided,
deploying different content to the same site).

The `marketing/public` content is kept in place for manual reference only — copy, structure, or ideas
worth carrying over, not for redeployment as-is.
